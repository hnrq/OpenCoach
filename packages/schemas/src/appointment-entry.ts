// Entry point for zod2md appointment schema documentation generation.
// Exports only appointment-related schemas so the generated doc is scoped for the coach agent.
export { AppointmentSchema } from "./index.ts";
